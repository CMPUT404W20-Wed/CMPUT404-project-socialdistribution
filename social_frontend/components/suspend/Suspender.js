import React from 'react';

import './suspend.css';


/* Progress indicator. */
const Suspender = () => (
  <div className="suspender">
    <span>·</span>
    <span>·</span>
    <span>·</span>
  </div>
);

export default Suspender;
